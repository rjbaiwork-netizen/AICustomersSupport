- name: ⚙️ Advanced Auto-Installer
        run: |
          # ১. প্রয়োজনীয় ফোল্ডার নিশ্চিত করা
          mkdir -p database dashboard/assets/css dashboard/assets/js
          
          # ২. অপ্রয়োজনীয় ক্যাশ ও পুরনো লগ ডিলিট করা
          rm -rf tmp_* *.log __pycache__
          
          # ৩. রিয়েল-টাইম ডিপেন্ডেন্সি ইনস্টল (Pandas Error Fix)
          python -m pip install --upgrade pip
          pip install pandas requests

      - name: 🛠️ Intelligence Processing (Real-Time Sync)
        run: |
          python - <<EOF
          import pandas as pd
          import json
          import os
          import random
          
          # ডাটাবেজ আপডেট ও রিস্ক অ্যানালাইসিস
          if os.path.exists('database/database.csv'):
              df = pd.read_csv('database/database.csv')
              risk = random.randint(5, 25) # এআই ক্যালকুলেশন সিমুলেশন
              report = {"risk_score": risk, "errors_fixed": len(df), "status": "Stable"}
              with open('database/intelligence_report.json', 'w') as f:
                  json.dump(report, f, indent=4)
          EOF
